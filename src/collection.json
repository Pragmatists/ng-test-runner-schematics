{
    "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
    "schematics": {
        "component": {
            "aliases": ["ngc"],
            "description": "Schematic that generates component with spec in ng-test-runner format",
            "factory": "./component",
            "schema": "./component/schema.json"
        },
        "ng-add": {
            "description": "Adds ng-test-runner-schematics to project (also with ng-test-runner dependency)",
            "factory": "./ng-add",
            "schema": "./ng-add/schema.json"
        }
    }
}
